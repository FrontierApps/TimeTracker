<h1>Editing part</h1>
<%= form_for :part, url: edit_job_timerecord_path(@timerecord), method: :patch do |f| %>
  <p>
  	<%= @job.JobNumber %>
  </p>
  <p>
    <%= f.label :taskname %><br>
    <%= f.collection_select :task_id, Task.order(:taskname),:id, :taskname, include_blank: true %>
  </p>
    
 
  <p>
    <%= f.submit "Time Out"%>	
  </p>
<% end %>